{
  "name": "orne",
  "private": true,
  "workspaces": [
    "apps/**/*",
    "packages/**/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev"
  },
  "packageManager": "yarn@3.4.1",
  "devDependencies": {
    "turbo": "^1.8.2"
  }
}
